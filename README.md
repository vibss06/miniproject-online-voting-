# Online Voting System (Python + Flask + SQL + HTML/JS)

## 🚀 How to Run Locally
1. Clone repo and open in VS Code.
2. Install requirements:
   ```bash
   pip install flask flask_sqlalchemy flask_cors werkzeug
