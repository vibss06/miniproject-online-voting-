// LOCAL DEV: keep this
// After backend is deployed on Render, replace with your Render URL.
window.BACKEND_URL = "http://127.0.0.1:5000";
// Example after deploy:
// window.BACKEND_URL = "https://YOUR-BACKEND.onrender.com";
